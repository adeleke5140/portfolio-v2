//we are gonna have an image rounded but
//then use a clipPath to add the online icon to its
//need to add idea of color switcher across the bottom as well.

import { cn } from '@/lib/utils'
import Image from 'next/image'

export const ProfileOnline = () => {
  let clipId = 'clip-id'
  return <div className="flex items-center justify-center"></div>
}
