# Fail `pnpm audit` on moderate-and-up advisories.
audit-level=moderate

# Quiet funding banner.
fund=false

# Leave lifecycle scripts enabled here so legitimate builds work;
# strictDepBuilds in pnpm-workspace.yaml is what actually gates them.
ignore-scripts=false
